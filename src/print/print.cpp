#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include <algorithm>
//#include "print.h"

using namespace std;

/* void printV(std::vector<vector<int>> graph)
{
    for_each(graph.begin(), graph.end(), [](vector<int>& vec_pogr)
    {
        for_each(vec_pogr.begin(),vec_pogr.end(), [](int i)
        {
            cout <<left << i;
        });
        cout << endl;
    });
} */


void printV(int n)
{
   cout << n;
}
    


